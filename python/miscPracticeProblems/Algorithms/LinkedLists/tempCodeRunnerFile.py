        # current_node = self.head
        # next_node = self.head.next

        # while next_node != None:
        #     if next_node == current_node:
        #         # make current_node's next, current next next
        # return current_node